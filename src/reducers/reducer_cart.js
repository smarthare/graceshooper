import { } from '../actions';

const initialState = {
  user: {},
  activeCart: {}
}

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
